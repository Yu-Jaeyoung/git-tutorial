# revert commands

- `git switch main`: 되돌릴 commit이 있는 브랜치로 이동합니다.
- `git revert --no-edit HEAD`: 현재 commit의 반대 내용을 가진 새 commit을 만듭니다. `--no-edit`는 기본 메시지를 그대로 사용합니다.

공통 명령과 표기는 [command-reference](../reference/README.md)를 참고합니다.
