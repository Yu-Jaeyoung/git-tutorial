# rebase commands

- `git switch feature/payment`: rebase 대상 브랜치를 `feature/payment`로 맞춥니다.
- `git rebase main`: 현재 브랜치 commit들을 최신 `main` 뒤에 다시 적용합니다.
- `git rebase --abort`: rebase 도중 conflict가 나거나 중단하고 싶을 때 시작 전 상태로 되돌립니다.

공통 명령과 표기는 [command-reference](../reference/README.md)를 참고합니다.
