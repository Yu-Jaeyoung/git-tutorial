# merge-commit commands

- `git switch main`: merge를 실행할 기준 브랜치를 `main`으로 맞춥니다.
- `git merge feature/login`: 현재 브랜치인 `main`에 `feature/login`을 병합합니다. 이 시나리오에서는 양쪽 브랜치가 모두 진행돼 있으므로 merge commit이 생깁니다.

공통 명령과 표기는 [command-reference](../reference/README.md)를 참고합니다.
