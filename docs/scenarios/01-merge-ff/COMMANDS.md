# merge-ff commands

- `git switch main`: fast-forward merge를 적용할 대상 브랜치를 `main`으로 맞춥니다.
- `git merge feature/login`: 현재 브랜치인 `main`에 `feature/login` 변경을 합칩니다. 이 시나리오에서는 새 merge commit 없이 포인터만 앞으로 이동합니다.

공통 명령과 표기는 [command-reference](../reference/README.md)를 참고합니다.
