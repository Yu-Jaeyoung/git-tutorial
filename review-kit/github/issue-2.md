# Draft login notice

## Background

고객 공지 초안을 준비하되, 중간 작업을 잠깐 치웠다가 다시 복원하는 흐름을 함께 연습합니다.

## Branch

`feature/login-notice`

## Tasks

- `docs/release-checklist.md`의 `customer notice`를 초안 상태로 수정
- `config.txt`에 `NOTICE_REVIEW=team-alpha` 추가
- `notes.txt`를 만들고, 중간에 `stash`를 사용해 작업을 잠깐 치웠다가 복원
- draft PR 생성

## Done when

- `feature/login-notice -> main` draft PR이 열려 있다
- 최종 commit에는 `notes.txt`가 포함되지 않는다
- 이 PR은 아직 merge하지 않는다
